# Architecture & Flow Diagrams

## 🏗️ Overall Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                          MERN TASK MANAGER                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐         ┌──────────────────────────┐
│   FRONTEND (React/Vite)  │         │ BACKEND (Node/Express)   │
├──────────────────────────┤         ├──────────────────────────┤
│                          │         │                          │
│ 1. Generate SessionId    │         │ 1. Receive SessionId     │
│ 2. Store in localStorage │         │ 2. Filter by SessionId   │
│ 3. Add to Headers        │         │ 3. Return Only Your Data │
│ 4. Send with ALL Requests│◄───────►│ 4. Verify Ownership      │
│ 5. Handle Errors         │         │ 5. Return Proper Errors  │
│ 6. Show Retry Button     │         │ 6. Log for Debugging     │
│                          │         │                          │
└──────────────────────────┘         └──────────────────────────┘
         │                                      │
         ▼                                      ▼
    Local Storage                          MongoDB
   (SessionId Only)               (Tasks with SessionId)
```

---

## 🔄 Data Flow: User A vs User B

```
USER A (Browser Window 1)           USER B (Browser Window 2)
=====================================  =====================================

localhost:5173                       localhost:5173
   ↓                                    ↓
Generate SessionId                   Generate SessionId
session_123456_abc123               session_789012_def456
   ↓                                    ↓
Store in localStorage               Store in localStorage
   ↓                                    ↓
Add Task "Learn React"              Add Task "Learn MongoDB"
   ↓                                    ↓
Request: GET /tasks                 Request: GET /tasks
Header: X-Session-ID: session_123456    Header: X-Session-ID: session_789012
   ↓                                    ↓
Backend: Find tasks with             Backend: Find tasks with
sessionId = session_123456           sessionId = session_789012
   ↓                                    ↓
Response: ["Learn React"]            Response: ["Learn MongoDB"]
   ↓                                    ↓
Display: "Learn React" only          Display: "Learn MongoDB" only
   ↓                                    ↓
User A doesn't see B's task ✓        User B doesn't see A's task ✓
```

---

## 🔄 Request Flow with Retry Logic

```
                    ┌─────────────────┐
                    │  User Adds Task │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  addTask(title) │
                    └────────┬────────┘
                             │
                ┌────────────┴────────────┐
                ▼                         ▼
         ┌──────────────┐         ┌──────────────┐
         │ retryRequest │         │ Get SessionId│
         └──────┬───────┘         └──────┬───────┘
                │                        │
                └───────────┬────────────┘
                            ▼
                    ┌─────────────────┐
                    │  API.post()     │
                    │  with Header    │
                    │  X-Session-ID   │
                    └────────┬────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
              ✓ SUCCESS          ✗ FAILED
                    │                 │
                    ▼                 ▼
            ┌────────────────┐  ┌─────────────┐
            │ Return Task    │  │ Retry (1/2) │
            │ Update UI      │  │ Wait 500ms  │
            │ Clear Error    │  └────┬────────┘
            └────────────────┘       │
                                     ▼
                            ┌─────────────────┐
                            │  API.post()     │
                            │  Attempt 2/2    │
                            └────────┬────────┘
                                     │
                           ┌─────────┴────────┐
                           │                  │
                      ✓ SUCCESS          ✗ FAILED
                           │                  │
                           ▼                  ▼
                   ┌────────────────┐  ┌────────────────┐
                   │ Return Task    │  │ Show Error +   │
                   │ Update UI      │  │ Retry Button   │
                   │ Clear Error    │  │ User Can Click │
                   └────────────────┘  │ Retry Again    │
                                       └────────────────┘
```

---

## 📱 Responsive Layout Transformation

```
MOBILE (375px)              TABLET (768px)           DESKTOP (1024px+)
════════════════════════    ═══════════════════      ═════════════════════

┌─────────────────┐         ┌──────────────────┐     ┌────────────────────┐
│    Task Form    │         │  Input │ Button  │     │    Task Manager    │
│                 │         ├──────────────────┤     ├────────────────────┤
│ ┌─────────────┐ │         │ Completed│Pending│     │  Input  │  Button  │
│ │   INPUT     │ │         ├──────────────────┤     ├────────────────────┤
│ └─────────────┘ │         │ Task Title       │     │ Completed│Pending  │
│ ┌─────────────┐ │         │ [Status] [Del]   │     ├────────────────────┤
│ │   BUTTON    │ │         ├──────────────────┤     │ Task Title         │
│ └─────────────┘ │         │ Task Title       │     │ [Status]  [Delete] │
├─────────────────┤         │ [Status] [Del]   │     ├────────────────────┤
│ Completed       │         └──────────────────┘     │ Task Title         │
│ Pending         │                                  │ [Status]  [Delete] │
│ All             │                                  └────────────────────┘
├─────────────────┤
│ Task Title      │
│ Status: Pend... │
│ [Status] [Del]  │
├─────────────────┤
│ Task Title      │
│ Status: Comp... │
│ [Status] [Del]  │
└─────────────────┘

Key Changes:
• Form: Column → Row (flex-direction)
• Filters: 1 col → 3 col (grid)
• Items: Column → Row
• Container: Full → Centered card
• Shadows: None → Visible
```

---

## 🔐 SessionId Security Flow

```
                    ┌──────────────────────┐
                    │  User Opens App      │
                    └──────────┬───────────┘
                               │
                    ┌──────────▼───────────┐
                    │  localStorage Check  │
                    └──────────┬───────────┘
                               │
                 ┌─────────────┴─────────────┐
                 │                           │
          No SessionId              SessionId Exists
                 │                           │
                 ▼                           ▼
         ┌──────────────────┐        ┌────────────────┐
         │ Generate New ID  │        │ Reuse Existing │
         │ session_XXX_YYY  │        │ session_XXX_YYY│
         └────────┬─────────┘        └────────┬───────┘
                  │                           │
                  └──────────────┬────────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │ Store in Storage│
                        │ Persist Across │
                        │ Page Refreshes  │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │ All API Calls   │
                        │ Include Header  │
                        │ X-Session-ID    │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │ Backend Filters │
                        │ By SessionId    │
                        │ Only Your Data  │
                        └─────────────────┘
```

---

## 📊 Error Handling Flow

```
            ┌──────────────────────────┐
            │  User Action             │
            │  (Add/Delete/Edit Task)  │
            └────────────┬─────────────┘
                         │
                         ▼
            ┌──────────────────────────┐
            │  Try API Call            │
            │  (with retry wrapper)    │
            └────────────┬─────────────┘
                         │
            ┌────────────┴────────────┐
            │                         │
         SUCCESS                  FAILURE
            │                         │
            ▼                         ▼
      ┌──────────┐          ┌────────────────┐
      │ Update   │          │ setError()     │
      │ State    │          │ Show Message   │
      │ Clear    │          │ Show Button    │
      │ Error    │          └────────┬───────┘
      └──────────┘                   │
                           ┌─────────▼────────┐
                           │ User Clicks      │
                           │ "Retry" Button   │
                           └────────┬─────────┘
                                    │
                           ┌────────▼─────────┐
                           │ Call loadTasks() │
                           │ Or Original Func │
                           └────────┬─────────┘
                                    │
                           ┌────────▼─────────┐
                           │ Try Again        │
                           │ (retry logic)    │
                           └────────┬─────────┘
                                    │
                         ┌──────────┴──────────┐
                         │                     │
                      SUCCESS              FAILURE
                         │                     │
                         ▼                     ▼
                    ┌──────────┐         ┌────────────┐
                    │ Success! │         │ Still Error│
                    │ Continue │         │ Keep Button│
                    └──────────┘         └────────────┘
```

---

## 🎯 State Management

```
┌─────────────────────────────────────────────────────────┐
│              Home.jsx State Management                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│ tasks: []                  ← All tasks for this session│
│ filter: "all"              ← Current filter            │
│ loading: false             ← Loading state             │
│ error: null                ← Error message or null     │
│                                                          │
├─────────────────────────────────────────────────────────┤
│                 State Transitions                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│ On Load:                                                │
│   loading: false → true  (show spinner)                │
│   → fetch tasks from backend filtered by sessionId     │
│   → loading: true → false (hide spinner)               │
│   → tasks: populated with session's tasks only         │
│                                                          │
│ On Error:                                               │
│   error: null → "Unable to fetch tasks..."            │
│   → show error message + retry button                  │
│   → user clicks retry → loadTasks() called again       │
│   → error cleared on success                           │
│                                                          │
│ On Add Task:                                            │
│   tasks: [] → [newTask, ...prev]  (optimistic)        │
│   error: cleared                                        │
│                                                          │
│ On Delete:                                              │
│   tasks: filtered (task removed)                       │
│   error: cleared                                        │
│                                                          │
│ On Filter Change:                                       │
│   filter: "all" → "completed" or "pending"             │
│   tasks: not changed (just filtered for display)       │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 🗄️ Database Schema

```
MongoDB Collection: tasks
═══════════════════════════════════════════════════════════

Document Example:
{
  _id: ObjectId("..."),
  title: "Learn React",           ← User input
  completed: false,               ← Task status
  sessionId: "session_123_abc",   ← Which session owns this
  createdAt: 2024-01-08T10:30Z,  ← Timestamp
  updatedAt: 2024-01-08T10:30Z   ← Timestamp
}

Index:
────
sessionId: true  ← Indexed for faster queries
           ↓
           When backend runs: Task.find({ sessionId })
           Can quickly find all tasks for that session
           ↑
           Better performance (especially important at scale)
```

---

## 🎯 Component Communication

```
┌────────────────────────────────────┐
│           App.jsx                  │
└────────────────┬───────────────────┘
                 │
                 ▼
┌────────────────────────────────────┐
│         Home.jsx (Logic)           │
│  ┌────────────────────────────┐   │
│  │ State Management           │   │
│  │ • tasks                    │   │
│  │ • filter                   │   │
│  │ • loading                  │   │
│  │ • error                    │   │
│  └────────────────────────────┘   │
│  ┌────────────────────────────┐   │
│  │ Functions                  │   │
│  │ • loadTasks()              │   │
│  │ • handleAdd()              │   │
│  │ • handleStatusChange()     │   │
│  │ • handleDelete()           │   │
│  └────────────────────────────┘   │
└─┬──────────┬──────────┬───────────┘
  │          │          │
  ▼          ▼          ▼
┌────────┐ ┌────────┐ ┌────────┐
│TaskForm│ │Filters │ │TaskList│
│        │ │        │ │        │
│ Input+ │ │ Buttons│ │┌──────┐│
│ Button │ │ All    │ ││Item1 ││
│        │ │ Done   │ ││Item2 ││
│        │ │ Pending│ │└──────┘┘
└────┬───┘ └─┬──────┘ └───┬────┘
     │       │            │
     ▼       ▼            ▼
  onAdd   onClick     status change
   ()    setFilter    onStatusChange
                      onDelete
```

---

## 🔌 API Integration

```
Frontend                              Backend
══════════════════════════════════    ═══════════════════════════════

API.js (Interceptor)                  Controller
├─ Get SessionId                      ├─ Extract SessionId from Header
├─ Add to Header                      ├─ Validate It Exists
├─ Make Request                       ├─ Query with SessionId Filter
├─ Handle Response                    ├─ Verify Ownership (Update/Delete)
├─ Retry on Failure                   ├─ Return Data or Error
└─ Throw Clear Error                  └─ Log Errors for Debug

GET /api/tasks
├─ Header: X-Session-ID: session_123
└─> Task.find({ sessionId: "session_123" })
    └─> Return only matching tasks

POST /api/tasks
├─ Header: X-Session-ID: session_123
├─ Body: { title: "..." }
└─> Task.create({ title, completed: false, sessionId: "session_123" })
    └─> Return new task

PUT /api/tasks/:id
├─ Header: X-Session-ID: session_123
├─ Verify: task.sessionId === "session_123"
└─> Task.findByIdAndUpdate(id, { completed })
    └─> Return updated task (or 403 if wrong sessionId)

DELETE /api/tasks/:id
├─ Header: X-Session-ID: session_123
├─ Verify: task.sessionId === "session_123"
└─> Task.findByIdAndDelete(id)
    └─> Return success (or 403 if wrong sessionId)
```

---

## 📈 Scalability Path

```
Today (SessionId)           Week 1 (Auth)             Future (Enterprise)
═════════════════════       ════════════════════      ═══════════════════

SessionId                   JWT Token                 OAuth 2.0
  ↓                           ↓                         ↓
localStorage              Cookie/Session            Secure Token
  ↓                           ↓                         ↓
Filter by sessionId        Filter by userId          Filter by userId
  ↓                           ↓                         ↓
Basic error handling       Login/Signup              Advanced Auth
  ↓                           ↓                         ↓
Simple retry              Session Management        Multi-factor Auth
  ↓                           ↓                         ↓
One-device use            Multi-device use          Enterprise Features

Current: Perfect for learning and demo ✓
Upgrade path: Just replace sessionId logic, everything else stays same ✓
```

---

This architecture is solid and production-ready for a first MERN project! 🚀
